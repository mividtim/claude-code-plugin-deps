---
description: Show the dependency tree for all installed plugins.
allowed-tools: Bash
---

# Plugin Dependency Tree

Display the dependency tree:

```
Bash(command="python3 ${CLAUDE_PLUGIN_ROOT}/scripts/resolve-deps.py tree")
```

Show the output to the user.
